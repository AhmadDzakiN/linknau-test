package questions

// Struct is a user defined type to store a collection of fields that groups together in a single name

type Person struct {
	Name string
	Age  uint // I use uint because age only has positive values
}
